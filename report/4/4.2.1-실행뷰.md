#### 4.2.1. 실행 뷰 (Runtime View)

실행 뷰는 런타임 상태의 컴포넌트 결합과 동작 흐름을 기술한다. Overall C&C View로 전체 정적 구조를 제시한 뒤, 요청 집중 구간의 주요 유스케이스를 동적 시퀀스로 확대한다. 각 시퀀스에서 어느 설계 전략이 작동하는지는 그림이 아니라 AS 적용 지점 표로 추적한다.

##### 4.2.1.1. Overall: 시스템 C&C View

front-api의 런타임 컴포넌트는 6개 계층으로 결합하며, 연결자는 네 종류(REST 동기 호출, DB 프로토콜, 캐시 조회, @Async 비동기 위임)다. 본 View는 회의 입장 백본(클라이언트 → 입장 Connector → domain.entry → join-pool → Primary)을 주축(굵은 화살표)으로 두고, 권한 캐시 경로와 비동기·선제 적재 경로를 그 위에 얹는다.

```mermaid
flowchart TB
    Client(["클라이언트<br/>미팅 웹 · PC 클라이언트"])

    subgraph EDGE["① Edge (AS-04)"]
        direction LR
        NGINX["G1 Nginx"]
        E1["E1 입장 Connector<br/>8081"]
        E2["E2 일반 Connector<br/>8080"]
        F1["F1 ThrottlingInterceptor<br/>AS-06"]
    end
    subgraph DOMAIN["② Domain (AS-01)"]
        direction LR
        D1["D1 domain.entry"]
        D2["D2 domain.auth"]
        D3["D3 domain.meeting"]
    end
    subgraph INTEG["③ Integration (AS-09) + 비동기 (AS-02)"]
        direction LR
        I1["I1 meetingManager"]
        I2["I2 ac"]
        I3["I3 copilot"]
        X1["X1 externalCallExecutor"]
    end
    subgraph CACHE["④ Cache (AS-03)"]
        direction LR
        L1["C1 L1 Caffeine"]
        L2["C2 L2 Redis"]
    end
    subgraph POOL["⑤ Bulkhead 풀 (AS-08)"]
        direction LR
        P1["P1 join"]
        P2["P2 service"]
        P3["P3 general"]
        P4["P4 query"]
    end
    subgraph DATA["⑥ Data (AS-07)"]
        direction LR
        PRI[("DB1 Primary")]
        REP[("DB2 Replica")]
    end
    S1["S1 PreWarmingScheduler<br/>AS-05"]
    subgraph EXT["외부 서버"]
        direction LR
        MM["Meeting Manager<br/>뒤단 cPaaS→WC / server-api→VC·AC"]
        ACS["AC서버"]
        CPS["Copilot Admin"]
    end

    Client --> NGINX
    NGINX -->|"/join·/conference-token"| E1
    NGINX -->|"그 외"| E2
    E1 --> D1
    E2 --> F1
    F1 --> D2 & D3
    D1 ==> P1
    D1 --> I1
    D2 --> L1
    L1 -->|miss| L2
    D2 --> I2 & I3
    D2 --> P3
    D3 --> P2
    D3 -->|readOnly| P4
    D3 --> I2
    I1 -.-> X1
    I2 -.-> X1
    I3 -.-> X1
    X1 -.-> MM & ACS & CPS
    P1 ==> PRI
    P2 ==> PRI
    P3 ==> PRI
    P4 --> REP
    S1 -.->|선제 적재| L2

    classDef edge fill:#e8f0ff,color:#000,stroke:#3b6cb5
    classDef dom fill:#e3f3e3,color:#000,stroke:#27ae60
    classDef integ fill:#fdf3e3,color:#000,stroke:#d68910
    classDef cache fill:#eef7f0,color:#000,stroke:#27ae60
    classDef pool fill:#fde8f0,color:#000,stroke:#c0392b
    classDef data fill:#f5f5f5,color:#000,stroke:#566573
    classDef ext fill:#ffffff,color:#000,stroke:#999,stroke-dasharray:4 4
    class NGINX,E1,E2,F1 edge
    class D1,D2,D3 dom
    class I1,I2,I3,X1 integ
    class L1,L2 cache
    class P1,P2,P3,P4 pool
    class PRI,REP data
    class MM,ACS,CPS ext
```
<!-- 이미지 파일명(draw.io → PNG 교체 시): report/images/4.2-overall-cnc.png -->
<p align="center"><em>[그림 46] 시스템 C&C View: 6계층 컴포넌트-커넥터 결합</em></p>

굵은 화살표는 회의 입장 백본(핵심 DB 경로), 점선은 @Async 비동기 위임, 실선은 REST 동기·캐시 조회다. 외부 서버 세 개(Meeting Manager · AC서버 · Copilot Admin)만 front-api가 직접 연계하며, WC·VC 서버는 Meeting Manager 뒤단이라 직접 결합이 아니다.

| ID | 컴포넌트 | 역할 | 관련 AS |
| ----- | ----- | ----- | :---: |
| G1 | Nginx | URL 패턴으로 입장(8081)과 그 외(8080)를 물리 분리 라우팅 | AS-04 |
| E1 | 입장 Connector | 입장 전용 Tomcat Connector. 단순 조회와 스레드 경합 없이 입장 스레드 예약 | AS-04 |
| E2 | 일반 Connector | 조회·권한 갱신·관리 수용. ThrottlingInterceptor가 앞단에 위치 | AS-04 |
| F1 | ThrottlingInterceptor | 피크 구간 중 비핵심 API 처리량 제한 | AS-06 |
| D1~D3 | domain.entry·auth·meeting | 입장·권한·회의 도메인. AS-01 경계로 분리 | AS-01 |
| I1~I3 | integration.* | Meeting Manager·AC·Copilot 연계(ACL), 서버별 차등 CB | AS-09 |
| X1 | externalCallExecutor | 외부 호출 전용 비동기 풀. 서블릿 스레드 즉시 반환 | AS-02 |
| C1·C2 | L1 Caffeine / L2 Redis | 로컬·분산 공유 계층 캐시로 외부 권한 호출 완충 | AS-03 |
| P1~P4 | join/service/general/query 풀 | 기능별 HikariCP Bulkhead | AS-08 |
| DB1·DB2 | Primary / Replica | Write·Read 전담. readOnly 트랜잭션으로 라우팅 | AS-07 |
| S1 | PreWarmingScheduler | 피크 N분 전 예약 회의 기반 L2 선제 적재 | AS-05 |
<p align="center"><em>[표 63] C&C View 컴포넌트별 역할</em></p>

데이터 흐름은 세 줄기다. (가) 입장 백본은 클라이언트부터 Primary까지의 핵심 동기 경로로 QA-02(8만 동시 입장)의 주 대상이다. (나) 권한 캐시는 L1·L2 조회로 외부 호출을 완충해 QA-01(권한 갱신 1초)을 지탱한다. (다) 비동기·선제 적재는 외부 호출을 X1이 전담하고 S1이 피크 진입 전 캐시를 채워, 어느 쪽도 동기 응답 경로에 끼어들지 않는다.

##### 4.2.1.2. UC-04 회의 입장

피크 시간대 8만 명 동시 입장 요청을 AS-04·AS-02·AS-08이 협력해 처리하는 흐름이다.

```mermaid
sequenceDiagram
    participant C as 클라이언트
    participant E as 입장 Connector
    participant D as domain.entry
    participant DB as MariaDB(join-pool)
    participant I as integration.meetingManager
    participant M as Meeting Manager

    C->>E: ① POST /meetings/{id}/join
    E->>D: ② 입장 요청 전달
    D->>DB: ③ 입장 가능 확인·conference-token 발급
    DB-->>D: 결과
    D->>I: ④ 참석자 입장 정보 조회
    I->>M: ⑤ 비동기 호출
    M-->>I: 입장 정보
    I-->>C: ⑥ 입장 파라미터 응답
```
<!-- 이미지 파일명(draw.io → PNG 교체 시): report/images/4.2-seq-uc04-join.png -->
<p align="center"><em>[그림 47] UC-04 회의 입장: 피크 집중 정상 처리 시퀀스</em></p>

| 스텝 | 지점 | 적용 AS | 효과 |
| :---: | ----- | :---: | ----- |
| ① | 입장 Connector 수신(8081) | AS-04 | 단순 조회·권한 갱신과 스레드 경합 차단, 입장 스레드 예약 |
| ③ | join-pool 커넥션 획득 | AS-08 | 입장 커넥션 고갈이 service·general 풀에 전파되지 않음 |
| ④ | integration.meetingManager(ACL) | AS-09 | CB 감지·fallback, 외부 API 스키마의 도메인 노출 차단 |
| ⑤ | externalCallExecutor 비동기 위임 | AS-02 | 서블릿 스레드 즉시 반환, 8만 건 동시 요청 수용 |
| ⑤ | CB Closed 상태 | AS-09 | 정상 시 Meeting Manager 직접 호출 |
<p align="center"><em>[표 64] UC-04 회의 입장 시나리오 AS 적용 지점</em></p>

##### 4.2.1.3. UC-01 권한 갱신: 캐시 hit/miss

로그인 후 권한 갱신 시 L1·L2 캐시 분기 흐름이다. 캐시 적중 경로(그림 48a)와 L2 miss 시 외부 권한 병렬 조회 경로(그림 48b)로 나눈다.

```mermaid
sequenceDiagram
    participant C as 클라이언트
    participant E as 일반 Connector
    participant F as ThrottlingInterceptor
    participant D as domain.auth
    participant L1 as L1 Caffeine
    participant L2 as L2 Redis

    C->>E: ① GET /members/{email}
    E->>F: ② 처리량 확인
    F->>D: ③ 통과 허용
    D->>L1: ④ L1 조회
    alt L1 hit
        L1-->>C: 권한 즉시 반환
    else L1 miss
        L1-->>D: miss
        D->>L2: ⑤ L2 조회
        alt L2 hit
            L2-->>D: 권한 반환
            D->>L1: L1 적재
            D-->>C: 응답
        else L2 miss
            L2-->>D: miss (그림 48b로 이어짐)
        end
    end
```
<!-- 이미지 파일명(draw.io → PNG 교체 시): report/images/4.2-seq-uc01-auth-a.png -->
<p align="center"><em>[그림 48a] UC-01 권한 갱신 (1/2): L1·L2 캐시 적중 경로 시퀀스</em></p>

```mermaid
sequenceDiagram
    participant D as domain.auth
    participant Iac as integration.ac
    participant Icp as integration.copilot
    participant AC as AC서버
    participant CP as Copilot Admin
    participant Cache as 캐시(L1·L2)

    par AC 권한
        D->>Iac: ⑥ AC 권한 조회
        Iac->>AC: ⑦ 비동기 호출
        AC-->>Iac: AC 권한
    and Copilot 권한
        D->>Icp: ⑥ Copilot 권한 조회
        Icp->>CP: ⑦ 비동기 호출
        CP-->>Icp: Copilot 권한
    end
    D->>Cache: ⑧ L1·L2 동시 적재
    D-->>D: ⑨ 권한 응답 반환
```
<!-- 이미지 파일명(draw.io → PNG 교체 시): report/images/4.2-seq-uc01-auth-b.png -->
<p align="center"><em>[그림 48b] UC-01 권한 갱신 (2/2): L2 miss 시 외부 권한 병렬 조회·적재 시퀀스</em></p>

| 스텝 | 지점 | 적용 AS | 효과 |
| :---: | ----- | :---: | ----- |
| ② | ThrottlingInterceptor | AS-06 | 피크 구간 비핵심 API 처리량 제한 |
| ④ | L1 Caffeine 조회 | AS-03 | 인스턴스 로컬 hit로 네트워크 없이 즉시 반환 |
| ⑤ | L2 Redis 조회 | AS-03 | 인스턴스 간 공유 캐시로 외부 중복 호출 방지 |
| ⑦ | externalCallExecutor 비동기 병렬 | AS-02 + AS-09 | AC·Copilot 병렬 조회, CB 보호 |
| ⑧ | L1·L2 동시 적재 | AS-03 | miss 후 외부 결과를 양 계층에 적재해 후속 hit 보장 |
<p align="center"><em>[표 65] UC-01 권한 갱신 시나리오 AS 적용 지점</em></p>

##### 4.2.1.4. AS-05 Pre-warming 선제 적재

피크 N분 전 PreWarmingScheduler가 L2 Redis를 선제 적재하고 ThrottlingInterceptor를 활성화하는 흐름이다. 서블릿 스레드와 완전히 분리된 스케줄 경로다.

```mermaid
sequenceDiagram
    participant S as PreWarmingScheduler
    participant DB as MariaDB
    participant P as PeakDetector
    participant EXT as 외부 서버
    participant L2 as L2 Redis

    S->>DB: ① 임박·대규모 예약 회의 조회
    DB-->>S: 대상 회의·참석자 목록
    S->>P: ② setActive(true) Throttling 활성화
    loop 50명/배치, 배치 간 100ms
        S->>EXT: ③ 참석자 권한 배치 선제 조회
        EXT-->>S: 권한 데이터
        S->>L2: ④ L2 선제 적재
    end
```
<!-- 이미지 파일명(draw.io → PNG 교체 시): report/images/4.2-seq-as05-prewarm.png -->
<p align="center"><em>[그림 49] AS-05 Pre-warming: 예약 기반 L2 Redis 선제 적재 및 Throttling 활성화 시퀀스</em></p>

| 스텝 | 지점 | 적용 AS | 효과 |
| :---: | ----- | :---: | ----- |
| ① | PreWarmingScheduler (preWarmExecutor, 1분 주기) | AS-05 | 예약 회의 기반 동적 피크 감지, 서블릿 스레드와 분리 |
| ② | PeakDetector setActive | AS-06 | 워밍 시작과 동시에 비핵심 API 처리량 제한 활성화 |
| ③ | 50명/배치 + 100ms 딜레이 | AS-05 | 워밍 호출이 외부 서버에 순간 부하를 주지 않도록 분산 |
| ④ | L2 선제 적재 | AS-05 + AS-03 | 피크 진입 시 cold start 없이 캐시 hit율 유지 |
<p align="center"><em>[표 66] AS-05 Pre-warming 시나리오 AS 적용 지점</em></p>

##### 4.2.1.5. AS-08 Bulkhead 격리

join-pool이 포화 상태여도 service-pool을 사용하는 회의 시작(UC-03)이 독립적으로 정상 처리됨을 보여준다. AS-01 도메인 경계가 DataSource 분리의 귀속 기준을 제공한다.

```mermaid
sequenceDiagram
    participant CJ as 입장 요청(8만)
    participant JP as join-pool
    participant CS as 회의 시작 요청
    participant SP as service-pool
    participant DB as MariaDB Primary

    CJ->>JP: ① 커넥션 요청
    JP-->>CJ: ② connectionTimeout 초과 → 획득 실패
    CS->>SP: ③ 커넥션 요청(독립 풀)
    SP->>DB: ④ 회의 시작 처리
    DB-->>SP: 정상 응답
    SP-->>CS: ⑤ 회의 시작 성공
```
<!-- 이미지 파일명(draw.io → PNG 교체 시): report/images/4.2-seq-as08-bulkhead.png -->
<p align="center"><em>[그림 50] AS-08 Bulkhead: join-pool 고갈 시 service-pool 독립 처리 시퀀스</em></p>

| 스텝 | 지점 | 적용 AS | 효과 |
| :---: | ----- | :---: | ----- |
| ② | join-pool 독립 DataSource | AS-08 | 입장 커넥션 고갈이 회의 시작·초대 커넥션에 영향 없음 |
| ③ | service-pool 독립 DataSource | AS-08 | join-pool 포화 상태와 무관하게 독립 운영 |
| ④~⑤ | QA-03 달성 구조 | AS-01 + AS-08 | domain 경계 기반 DataSource 분리로 물리 격리, 성공률 100% |
<p align="center"><em>[표 67] AS-08 Bulkhead 격리 시나리오 AS 적용 지점</em></p>

##### 4.2.1.6. AS-09 Circuit Breaker: 서버별 차등 fallback

외부 서버 장애 시 CB Open 전환과 서버별 차등 fallback 흐름이다. 필수 서버(Meeting Manager)의 fail-fast(그림 51a)와 Fallback 가능 서버(Copilot Admin)의 계층적 복구(그림 51b)로 나눈다.

```mermaid
sequenceDiagram
    participant C as 클라이언트
    participant D as domain.entry
    participant I as integration.meetingManager
    participant M as Meeting Manager

    C->>D: ① POST /meetings/{id}/join
    D->>I: ② 참석자 입장 정보 조회
    I->>M: ③ 호출(실패율 누적 → threshold)
    M-->>I: 오류 응답
    I-->>D: ④ CB Open → fail-fast, 오류 반환
    D-->>C: ⑤ 입장 실패 응답(원인 메시지)
```
<!-- 이미지 파일명(draw.io → PNG 교체 시): report/images/4.2-seq-as09-cb-a.png -->
<p align="center"><em>[그림 51a] AS-09 Circuit Breaker (1/2): Meeting Manager 장애 시 CB Open·fail-fast 시퀀스</em></p>

```mermaid
sequenceDiagram
    participant C as 클라이언트
    participant D as domain.auth
    participant I as integration.copilot
    participant L2 as L2 Redis
    participant DB as MariaDB

    C->>D: ① GET /members/{email}
    D->>I: ② 권한 조회
    I-->>I: ③ CB Open → fail-fast
    I->>L2: ④ L2 Fallback 조회
    alt L2 hit
        L2-->>D: 마지막 적재값
        D-->>C: ⑤ 정상 응답(연속성 유지)
    else L2 miss
        L2-->>I: miss
        I->>DB: ⑥ DB 저장값 Fallback
        DB-->>D: 권한 데이터
        D-->>C: ⑤ 정상 응답
    end
```
<!-- 이미지 파일명(draw.io → PNG 교체 시): report/images/4.2-seq-as09-cb-b.png -->
<p align="center"><em>[그림 51b] AS-09 Circuit Breaker (2/2): Copilot Admin 장애 시 CB Open·계층적 Fallback(L2→DB) 시퀀스</em></p>

| 스텝 | 지점 | 적용 AS | 효과 |
| :---: | ----- | :---: | ----- |
| 51a-④ | meetingManager CB Open → fail-fast | AS-09 | Meeting Manager 장애 시 timeout 없이 즉시 거부, 스레드 블로킹 방지 |
| 51b-③④ | copilotAdmin CB Open → L2 Fallback | AS-09 + AS-03 | Copilot Admin 장애 시 캐시 기반 계층 복구 |
| 51b-⑥ | DB 저장값 최종 Fallback | AS-09 | L2 miss 시 DB 저장 권한값으로 서비스 연속성 유지 |
| 전체 | 서버별 독립 CB 정책 | AS-09 | Meeting Manager 50%/10s, AC서버 60%/30s, Copilot Admin 70%/60s 차등 |
<p align="center"><em>[표 68] AS-09 Circuit Breaker 시나리오 AS 적용 지점</em></p>
