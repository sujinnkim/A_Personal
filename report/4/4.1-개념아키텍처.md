### 4.1. 개념 아키텍처

#### 4.1.1. 아키텍처 개념도

3.2절의 설계 전략(AS-01~AS-11)이 적용된 To-Be 아키텍처의 전체 구조다. 요청 집중 구간 대응을 중심으로 각 전략이 어느 경계에서 동작하는지를 시각화한다.

```mermaid
flowchart TD
    subgraph CLIENT["클라이언트"]
        direction LR
        WEB["미팅 웹포탈 · 모바일 앱"]
    end

    subgraph ROUTING["AS-04  요청 우선순위 분기"]
        direction LR
        JOIN_C["입장 전용 Connector\nport 8081 · 전용 스레드 풀\n/join · /conference-token"]
        GEN_C["일반 Connector\nport 8080\n조회 · 권한 갱신 · 관리"]
    end

    subgraph FRONTAPI["front-api"]
        THROT["AS-06  Throttling Filter\n피크 구간 중 비핵심 API 처리량 제한"]

        subgraph DOMAIN["AS-01  도메인 모듈 레이어"]
            direction LR
            ENTRY["domain.entry\n입장 처리"]
            AUTH_D["domain.auth\n권한 갱신"]
            MEET["domain.meeting\n회의 관리"]
        end

        subgraph ACL_MOD["AS-11  ACL 연계 모듈 레이어"]
            direction LR
            WC_INT["integration.wc\nAS-10 CB · Fallback"]
            AC_INT["integration.ac\nAS-10 CB · Fallback"]
            CP_INT["integration.copilot\nAS-10 CB · Fallback"]
        end

        ASYNC["AS-02  AsyncTaskExecutor\n외부 호출 전용 스레드 풀\nAS-05  Eager Response"]
    end

    subgraph CACHE_LAYER["AS-03  캐시 계층"]
        direction LR
        L1["L1 Caffeine\n인스턴스 로컬 · TTL 5분"]
        L2["L2 Redis\n분산 공유 · TTL 30분~1시간"]
    end

    PREWARM["AS-07  Pre-warming Scheduler\n예약 회의 기반 동적 워밍\n피크 N분 전 L2 선제 적재"]

    subgraph BULKHEAD["AS-09  HikariCP Bulkhead"]
        direction LR
        JP["join-pool\n50 conn"]
        SP["service-pool\n30 conn"]
        GP["general-pool\n30 conn"]
        QP["query-pool  Read 전용\n30 conn"]
    end

    subgraph DB_LAYER["MariaDB  ·  AS-08  CQRS"]
        direction LR
        PRI["Primary\nWrite"]
        REP["Replica\nRead"]
    end

    subgraph EXT["외부 서버"]
        direction LR
        WCS["WC서버"]
        ACS["AC서버"]
        CPS["Copilot Admin"]
    end

    subgraph FEEDBACK["cPaaS 피드백 흐름  역방향"]
        direction LR
        CPAAS["cPaaS"]
        SAPI["server-api"]
    end

    %% ── 요청 진입 ──────────────────────────────
    WEB -->|입장 요청| JOIN_C
    WEB -->|일반 요청| GEN_C
    JOIN_C & GEN_C --> THROT
    THROT --> ENTRY & AUTH_D & MEET

    %% ── 캐시 흐름 ──────────────────────────────
    AUTH_D -->|L1 조회| L1
    L1 --->|L1 미스| L2
    L2 --->|L2 미스 → 외부 호출 후 적재| CP_INT
    PREWARM -->|선제 적재| L2

    %% ── ACL → Async → 외부 서버 ───────────────
    ENTRY --> WC_INT
    MEET --> AC_INT
    AUTH_D -.->|권한 갱신 시| CP_INT
    WC_INT & AC_INT & CP_INT --> ASYNC
    ASYNC -->|비동기 호출| WCS & ACS & CPS

    %% ── Bulkhead 연결 ───────────────────────────
    ENTRY --> JP
    MEET --> SP
    AUTH_D --> GP
    MEET -->|readOnly 조회| QP

    %% ── DB 연결 ─────────────────────────────────
    JP & SP & GP --> PRI
    QP --> REP

    %% ── cPaaS 피드백 흐름 ───────────────────────
    CPAAS --> SAPI --> JP
```
<!-- 이미지 파일명(draw.io → PNG 교체 시): report/images/4.1-tobe-architecture.png -->

---

#### 주요 설계 결정 요약

| 경계 | 적용 전략 | 해결 이슈 |
|------|---------|---------|
| 요청 진입 분기 | AS-04 우선순위 큐 · AS-06 Throttling | ISSUE-01, ISSUE-03, ISSUE-09 |
| 도메인 모듈 경계 | AS-01 도메인 분리 | ISSUE-04, ISSUE-07, ISSUE-08 |
| 캐시 계층 | AS-03 L1+L2 캐시 · AS-07 Pre-warming | ISSUE-02, ISSUE-05, ISSUE-09 |
| 외부 연계 경계 | AS-11 ACL · AS-10 Circuit Breaker | ISSUE-06, ISSUE-08 |
| 외부 호출 스레드 | AS-02 Async · AS-05 Eager Response | ISSUE-01, ISSUE-05, ISSUE-06 |
| 커넥션 풀 격리 | AS-09 HikariCP Bulkhead | ISSUE-01, ISSUE-04, ISSUE-06 |
| DB 읽기/쓰기 분리 | AS-08 경량 CQRS (Primary/Replica) | ISSUE-07 |

---

#### 흐름별 설명

**주 요청 흐름 (회의 입장)**

1. 클라이언트가 `/join` 요청을 전송한다.
2. **AS-04**: 입장 전용 Connector(port 8081)가 전용 스레드 풀에서 요청을 수신한다.
3. **AS-06**: 피크 구간 중 일반 요청은 Throttling이 처리량을 제한한다. 입장 요청은 면제.
4. **AS-01**: `domain.entry`가 입장 처리를 담당한다.
5. **AS-11 + AS-10**: `integration.meetingManager`(ACL 모듈)를 통해 Meeting Manager를 호출한다. CB가 장애를 감지하면 fallback 처리.
6. **AS-02 + AS-05**: Meeting Manager 호출은 `AsyncTaskExecutor`에서 비동기 실행. 서블릿 스레드는 Meeting Manager 전달을 비동기로 위임하고 즉시 반환.
7. **AS-09**: `domain.entry` → `join-pool` → MariaDB Primary. 다른 기능의 풀과 격리.

**권한 갱신 흐름**

1. 로그인 후 `GET /members/{email}` 호출.
2. **AS-03**: `domain.auth`가 L1 Caffeine 조회 → 미스 시 L2 Redis 조회.
3. L2도 미스인 경우에만 `integration.copilot`(ACL)을 통해 Copilot Admin 서버 호출 후 L1·L2 동시 적재.
4. **AS-07**: Pre-warming Scheduler가 피크 N분 전에 대상 참석자 권한을 L2에 선제 적재하여 cold start 방지.

**cPaaS 피드백 흐름 (역방향)**

- `cPaaS → server-api → join-pool → MariaDB Primary`
- 참석자 상태 변경(퇴장, 연결 끊김)이 사용자 요청과 독립적으로 발생한다.
- **AS-08**: 참석자 목록 조회(read)는 `query-pool → Replica`로 라우팅하여 피드백 write와 lock 경합을 분리한다.

---

#### 4.1.2. 설계 전략 적용 범위

3.2절의 설계 전략(AS-01~AS-11)이 아키텍처의 어느 레이어·컴포넌트에 적용되는지를 정리한다.

**설계 전략 적용 매핑**

| AS | 전략명 | 적용 레이어 | 적용 컴포넌트 | 관련 드라이버 |
|----|-------|-----------|------------|------------|
| AS-01 | 도메인 모듈 분리 | 애플리케이션 | domain.entry · domain.auth · domain.meeting | AD-03 |
| AS-02 | 비동기 처리 | 애플리케이션 | AsyncTaskExecutor (외부 호출 전용 스레드 풀) | AD-02, AD-04 |
| AS-03 | 캐시 기반 응답 속도 개선 | 캐시 | L1 Caffeine (TTL 5분) · L2 Redis (TTL 30분~1시간) | AD-01 |
| AS-04 | 요청 우선순위 분기 | 요청 진입 | 입장 전용 Connector (port 8081) · 일반 Connector (port 8080) | AD-02 |
| AS-05 | Eager Response + 비동기 후처리 | 애플리케이션 | AsyncTaskExecutor (AS-02 기반) · domain.entry 응답 반환 시점 | AD-02, AD-04 |
| AS-06 | Time-based Throttling | 요청 진입 | Throttling Filter (front-api 공통 필터 체인) | AD-02, AD-04 |
| AS-07 | Predictive Pre-warming | 캐시 / 인프라 | Pre-warming Scheduler · L2 Redis (AS-03 캐시 인프라 전제) | AD-01, AD-02, AD-04 |
| AS-08 | 경량 CQRS | DB 접근 | query-pool → MariaDB Replica · 나머지 풀 → Primary | AD-03 |
| AS-09 | HikariCP Bulkhead | DB 접근 | join-pool (50conn) · service-pool (30conn) · general-pool (30conn) · query-pool (30conn) | AD-03, AD-04 |
| AS-10 | Circuit Breaker / Fallback | 외부 연계 | integration.wc · integration.ac · integration.copilot (CB + Fallback) | AD-04 |
| AS-11 | Anti-Corruption Layer | 외부 연계 | integration.wc · integration.ac · integration.copilot (ACL 구조, AS-10과 동일 모듈) | AD-09, AD-10 |

**레이어별 전략 요약**

| 레이어 | 적용 전략 | 핵심 목적 |
|-------|---------|---------|
| 요청 진입 | AS-04, AS-06 | 피크 시 입장 요청 우선 처리 · 비핵심 API 처리량 제한 |
| 애플리케이션 | AS-01, AS-02, AS-05 | 도메인 경계 설정 · 외부 호출 비동기화 · 서블릿 스레드 조기 반환 |
| 캐시 | AS-03, AS-07 | 외부 호출 대체 · 피크 전 선제 적재로 cold start 방지 |
| 외부 연계 | AS-10, AS-11 | 외부 서버 장애 격리 · 연계별 독립 정책 적용 |
| DB 접근 | AS-08, AS-09 | 기능별 커넥션 풀 격리 · Read/Write DB 분리 |

---

#### 4.1.3. 구현 및 검증 대상

**개념도 표기 범례**

| 표기 | 의미 |
|-----|-----|
| `subgraph 이름["레이블"]` | 아키텍처 경계 · 레이어 |
| 노드 레이블의 `AS-XX` 접두사 | 해당 컴포넌트에 적용된 설계전략 |
| `-->` 실선 화살표 | 주 요청 · 응답 흐름 |
| `--->` 긴 실선 화살표 | 캐시 조회 흐름 |
| `-.->` 점선 화살표 | 조건부 · 선택적 흐름 |
| `style ... fill:#ff9999` | 오류 · 고갈 상태 노드 |

**전체 설계 범위 vs 프로토타입 구현 범위**

O: 프로토타입에서 구현 및 시나리오 기반 검증 / △: 구현하되 전용 검증 시나리오 없이 동작으로 확인

| AS | 전략명 | 프로토타입 구현 | 검증 항목 | 비고 |
|----|-------|:----------:|:------:|-----|
| AS-01 | 도메인 모듈 분리 | △ | — | 패키지 경계 구조로 확인 |
| AS-02 | 비동기 처리 | O | IV-01 | |
| AS-03 | 캐시 기반 응답 속도 개선 | O | IV-03 | L1 Caffeine + L2 Redis 모두 구현 |
| AS-04 | 요청 우선순위 분기 | O | IV-01 | |
| AS-05 | Eager Response + 비동기 후처리 | O | IV-04 | |
| AS-06 | Time-based Throttling | △ | — | IV-01 시나리오 중 비핵심 요청 제한 동작으로 확인 |
| AS-07 | Predictive Pre-warming | O | IV-03 | |
| AS-08 | 경량 CQRS | △ | — | IV-01 시나리오 중 Replica 라우팅 동작으로 확인 |
| AS-09 | HikariCP Bulkhead | O | IV-01, IV-02 | 핵심 검증 대상 |
| AS-10 | Circuit Breaker / Fallback | O | IV-04 | 외부 서버 Stub 대체 (C-05) |
| AS-11 | Anti-Corruption Layer | △ | — | 외부 서버 Stub 대체 (C-05), 모듈 구조로 확인 |

**검증 항목 (IV)**

각 IV는 아키텍처 품질 요구사항(QA)의 달성 여부를 프로토타입으로 검증한다.

| IV | 검증 목표 | 관련 QA | 관련 AS | 검증 방법 | 프로토타입 제약 |
|----|---------|--------|--------|---------|-------------|
| IV-01 | 동시 입장 집중 구간에서 DB 커넥션 풀 사용률 80% 이하, 평균 응답시간 1초 이내 | QA-02 | AS-02, AS-04, AS-09 | k6 부하 · HikariCP activeConnections 메트릭 · 응답시간 분포 | C-06: 2만 명 축소 시뮬레이션 (단일 서버) |
| IV-02 | join-pool 고갈 시 service-pool 격리 — 회의 조회 API 성공률 100% 유지 | QA-03 | AS-09 | join-pool 의도적 포화 후 조회 API 동시 호출 · 성공률 확인 | — |
| IV-03 | 피크 시 권한 갱신 응답시간 1초 이내 — 캐시 히트율 90% 이상 | QA-01 | AS-03, AS-07 | k6 로그인 부하 · L1/L2 캐시 히트율 · 외부 호출 횟수 비교 | — |
| IV-04 | 외부 서버 장애 주입 시 핵심 기능 성공률 99.9% 이상 | QA-04, QA-05 | AS-05, AS-10 | Stub 장애 주입 (응답 지연 / 오류) 후 입장 · 조회 API 성공률 측정 | C-05: 외부 서버 Stub 대체 |
