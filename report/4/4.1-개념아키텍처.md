### 4.1. 개념 아키텍처

#### 4.1.1. 아키텍처 개념도

3.2절의 설계 전략(AS-01~AS-09)이 적용된 To-Be 아키텍처의 전체 구조다. 요청 집중 구간 대응을 중심으로 각 전략이 어느 경계에서 동작하는지를 시각화한다.

```mermaid
flowchart TD
    subgraph CLIENT["클라이언트"]
        direction LR
        WEB["미팅 웹포탈 · 모바일 앱"]
    end

    subgraph ROUTING["AS-04  요청 우선순위 분기"]
        direction LR
        JOIN_C["입장 전용 Connector\nport 8081 · 전용 스레드 풀\n/join · /conference-token"]
        GEN_C["일반 Connector\nport 8080\n조회 · 권한 갱신 · 관리"]
    end

    subgraph FRONTAPI["front-api"]
        THROT["AS-05  Throttling Filter\n피크 구간 중 비핵심 API 처리량 제한"]

        subgraph DOMAIN["AS-01  도메인 모듈 레이어"]
            direction LR
            ENTRY["domain.entry\n입장 처리"]
            AUTH_D["domain.auth\n권한 갱신"]
            MEET["domain.meeting\n회의 관리"]
        end

        subgraph ACL_MOD["ACL 연계 모듈 레이어"]
            direction LR
            MM_INT["integration.meetingManager\nAS-09 CB · Fallback"]
            AC_INT["integration.ac\nAS-09 CB · Fallback"]
            CP_INT["integration.copilot\nAS-09 CB · Fallback"]
        end

        ASYNC["AS-02  AsyncTaskExecutor\n외부 호출 전용 스레드 풀"]

        PREWARM["AS-06  Pre-warming Scheduler\n예약 회의 기반 동적 워밍\n피크 N분 전 L2 선제 적재"]
    end

    subgraph CACHE_LAYER["AS-03  캐시 계층"]
        direction LR
        L1["L1 Caffeine\n인스턴스 로컬 · TTL 5분"]
        L2["L2 Redis\n분산 공유 · TTL 30분~1시간"]
    end

    subgraph BULKHEAD["AS-08  HikariCP Bulkhead"]
        direction LR
        JP["join-pool\n50 conn"]
        SP["service-pool\n30 conn"]
        GP["general-pool\n30 conn"]
        QP["query-pool  Read 전용\n30 conn"]
    end

    subgraph DB_LAYER["MariaDB  ·  AS-07  CQRS"]
        direction LR
        PRI["Primary\nWrite"]
        REP["Replica\nRead"]
    end

    subgraph EXT["외부 서버"]
        direction LR
        MM["Meeting Manager"]
        ACS["AC서버"]
        CPS["Copilot Admin"]
    end

    subgraph FEEDBACK["cPaaS 피드백 흐름  역방향"]
        direction LR
        CPAAS["cPaaS"]
        SAPI["server-api"]
    end

    %% ── 요청 진입 ──────────────────────────────
    WEB -->|입장 요청| JOIN_C
    WEB -->|일반 요청| GEN_C
    JOIN_C --> ENTRY
    GEN_C --> THROT
    THROT --> AUTH_D & MEET

    %% ── 캐시 흐름 ──────────────────────────────
    AUTH_D -->|L1 조회| L1
    L1 --->|L1 miss| L2
    L2 --->|L2 miss → 외부 호출 후 적재| AC_INT & CP_INT
    PREWARM -->|선제 적재| L2

    %% ── ACL → Async → 외부 서버 ───────────────
    %% draw.io 권장: MM_INT·AC_INT·CP_INT → ASYNC 3선을 ACL 레이어 경계 → ASYNC 1선으로 표현
    ENTRY --> MM_INT
    MEET --> AC_INT
    MM_INT & AC_INT & CP_INT --> ASYNC
    ASYNC -->|비동기 호출| MM & ACS & CPS

    %% ── Bulkhead 연결 ───────────────────────────
    ENTRY --> JP
    MEET --> SP
    AUTH_D --> GP
    MEET -->|readOnly 조회| QP

    %% ── DB 연결 ─────────────────────────────────
    %% draw.io 권장: JP·SP·GP → PRI 3선을 Bulkhead write 측 경계 → Primary 1선으로 표현
    JP & SP & GP --> PRI
    QP --> REP

    %% ── cPaaS 피드백 흐름 ───────────────────────
    CPAAS --> SAPI --> JP
```

![TO-BE 미팅 포털 서버 아키텍처 개념도](..\..\report\images\4.1-tobe-architecture.png)
<p align="center"><em>[그림 45] TO-BE 미팅 포털 서버 아키텍처 개념도</em></p>

#### 레이어 및 모듈 설명

| 레이어 | 모듈 | 설명 | 연관 AS |
| ----- | ----- | ----- | :---: |
| 요청 진입 | 입장 전용 Connector (port 8081) | 회의 입장·conference-token 전용 독립 스레드 풀로 수신; Throttling Filter 경유 없음 | AS-04 |
| 요청 진입 | 일반 Connector (port 8080) | 조회·권한 갱신 등 일반 요청 수신; Throttling Filter 통과 후 처리 | AS-04 |
| 요청 진입 | Throttling Filter | 피크 구간 중 비핵심 API 처리량 제한; `@ThrottleExempt` 경로는 면제 | AS-05 |
| 도메인 모듈 | domain.entry | 회의 입장 처리 — conference-token 발급, 입장 파라미터 조립 | AS-01 |
| 도메인 모듈 | domain.auth | 권한 갱신 처리 — AC·LLM·용어사전 권한 정보 로드 및 캐시 적재 | AS-01 |
| 도메인 모듈 | domain.meeting | 회의 개설·조회·예약·종료 처리 | AS-01 |
| ACL 연계 모듈 | integration.meetingManager | Meeting Manager Feign 호출 캡슐화; 서버별 독립 CB·Fallback 적용 | AS-09 |
| ACL 연계 모듈 | integration.ac | AC서버 Feign 호출 캡슐화; 서버별 독립 CB·Fallback 적용 | AS-09 |
| ACL 연계 모듈 | integration.copilot | Copilot Admin Feign 호출 캡슐화; 서버별 독립 CB·Fallback 적용 | AS-09 |
| 비동기 실행 | AsyncTaskExecutor | 외부 서버 호출 전용 스레드 풀; 서블릿 스레드를 즉시 반환하고 `CompletableFuture`로 결과 합산 | AS-02 |
| 선제 초기화 | Pre-warming Scheduler | 예약 회의 기반 피크 N분 전 L2 Redis 선제 적재; AS-05 Throttling 동시 활성화 | AS-06 |
| 캐시 | L1 Caffeine | 인스턴스 로컬 캐시 (TTL 5분); 네트워크 없이 즉시 응답 | AS-03 |
| 캐시 | L2 Redis | 분산 공유 캐시 (TTL 30분~1시간); 인스턴스 간 공유, Pre-warming 적재 대상 | AS-03 |
| DB 접근 | join-pool / service-pool / general-pool / query-pool | 기능별 독립 HikariCP 커넥션 풀; 풀 간 격리로 장애 전파 차단 | AS-08 |
| DB 접근 | MariaDB Primary | Write 전담 — 입장·개설·상태 변경 등 Command 경로 | AS-07 |
| DB 접근 | MariaDB Replica | Read 전담 — 참석자 목록·회의 조회 등 Query 경로 | AS-07 |
<p align="center"><em>[표 57] 아키텍처 개념도 레이어 및 모듈 설명</em></p>

#### 주요 설계 결정 요약

| 경계 | 적용 전략 | 해결 이슈 |
| ----- | ----- | ----- |
| 요청 진입 분기 | AS-04 우선순위 큐 · AS-05 Throttling | ISSUE-01, ISSUE-03, ISSUE-09 |
| 도메인 모듈 경계 | AS-01 도메인 분리 | ISSUE-04, ISSUE-07, ISSUE-08 |
| 캐시 계층 | AS-03 L1+L2 캐시 · AS-06 Pre-warming | ISSUE-02, ISSUE-05, ISSUE-09 |
| 외부 연계 경계 | AS-09 Circuit Breaker | ISSUE-02, ISSUE-06, ISSUE-08 |
| 외부 호출 스레드 | AS-02 Async | ISSUE-01, ISSUE-05, ISSUE-06 |
| 커넥션 풀 격리 | AS-08 HikariCP Bulkhead | ISSUE-01, ISSUE-04, ISSUE-06 |
| DB 읽기/쓰기 분리 | AS-07 경량 CQRS (Primary/Replica) | ISSUE-07 |
<p align="center"><em>[표 58] 주요 설계 결정 요약</em></p>

#### 4.1.2. 설계 전략 적용 범위

3.2절의 설계 전략(AS-01~AS-09)이 아키텍처의 어느 레이어·컴포넌트에 적용되는지를 정리한다.

**설계 전략 적용 매핑**

| AS | 전략명 | 적용 레이어 | 적용 컴포넌트 | 관련 드라이버 |
| :---: | ----- | ----- | ----- | ----- |
| AS-01 | 입장 처리 도메인 경계 분리 | 애플리케이션 | domain.entry · domain.auth · domain.meeting | AD-03 |
| AS-02 | 입장 처리 경로 비동기 전환 | 애플리케이션 | AsyncTaskExecutor (외부 호출 전용 스레드 풀) | AD-02, AD-04 |
| AS-03 | 외부 권한 조회 다층 캐시 적용 | 캐시 | L1 Caffeine (TTL 5분) · L2 Redis (TTL 30분~1시간) | AD-01 |
| AS-04 | 입장 전용 처리 경로 확보 | 요청 진입 | 입장 전용 Connector (port 8081) · 일반 Connector (port 8080) | AD-02 |
| AS-05 | 피크 구간 요청 유입 제한 (Time-based Throttling) | 요청 진입 | Throttling Filter (front-api 공통 필터 체인) | AD-02, AD-04 |
| AS-06 | 예약 기반 피크 자원 선제 초기화 (Predictive Pre-warming) | 캐시 / 인프라 | Pre-warming Scheduler · L2 Redis (AS-03 캐시 인프라 전제) | AD-01, AD-02, AD-04 |
| AS-07 | 조회·입장 DB 경로 분리 (CQRS) | DB 접근 | query-pool → MariaDB Replica · 나머지 풀 → Primary | AD-03 |
| AS-08 | 기능별 커넥션·스레드 격벽 분리 (Bulkhead) | DB 접근 | join-pool (50conn) · service-pool (30conn) · general-pool (30conn) · query-pool (30conn) | AD-03, AD-04 |
| AS-09 | 외부 서버 장애 차단 및 계층 복구 (Circuit Breaker) | 외부 연계 | integration.meetingManager · integration.ac · integration.copilot (CB + Fallback) | AD-04 |
<p align="center"><em>[표 59] 설계 전략 적용 매핑</em></p>

**레이어별 전략 요약**

| 레이어 | 적용 전략 | 핵심 목적 |
| ----- | ----- | ----- |
| 요청 진입 | AS-04, AS-05 | 피크 시 입장 요청 우선 처리 · 비핵심 API 처리량 제한 |
| 애플리케이션 | AS-01, AS-02 | 도메인 경계 설정 · 외부 호출 비동기화 |
| 캐시 | AS-03, AS-06 | 외부 호출 대체 · 피크 전 선제 적재로 cold start 방지 |
| 외부 연계 | AS-09 | 외부 서버 장애 격리 · 연계별 독립 정책 적용 |
| DB 접근 | AS-07, AS-08 | 기능별 커넥션 풀 격리 · Read/Write DB 분리 |
<p align="center"><em>[표 60] 레이어별 설계 전략 요약</em></p>

#### 4.1.3. 구현 및 검증 대상

**전체 설계 범위 vs 프로토타입 구현 범위**

O: 프로토타입에서 구현 및 시나리오 기반 검증 / △: 구현하되 전용 검증 시나리오 없이 동작으로 확인

| AS | 전략명 | 프로토타입 구현 | 검증 항목 | 비고 |
| :---: | ----- | :---: | :---: | ----- |
| AS-01 | 입장 처리 도메인 경계 분리 | △ | — | 패키지 경계 구조로 확인 |
| AS-02 | 입장 처리 경로 비동기 전환 | O | IV-01 | |
| AS-03 | 외부 권한 조회 다층 캐시 적용 | O | IV-03 | L1 Caffeine + L2 Redis 모두 구현 |
| AS-04 | 입장 전용 처리 경로 확보 | O | IV-01 | |
| AS-05 | 피크 구간 요청 유입 제한 (Time-based Throttling) | △ | — | IV-01 시나리오 중 비핵심 요청 제한 동작으로 확인 |
| AS-06 | 예약 기반 피크 자원 선제 초기화 (Predictive Pre-warming) | O | IV-03 | |
| AS-07 | 조회·입장 DB 경로 분리 (CQRS) | △ | — | IV-01 시나리오 중 Replica 라우팅 동작으로 확인 |
| AS-08 | 기능별 커넥션·스레드 격벽 분리 (Bulkhead) | O | IV-01, IV-02 | 핵심 검증 대상 |
| AS-09 | 외부 서버 장애 차단 및 계층 복구 (Circuit Breaker) | O | IV-04 | 외부 서버 Stub 대체 (C-05) |
<p align="center"><em>[표 61] 전체 설계 범위 vs 프로토타입 구현 범위</em></p>

**검증 항목 (IV: Implementation Verification)**

각 IV는 아키텍처 품질 요구사항(QA)의 달성 여부를 프로토타입으로 검증한다.

| IV | 검증 목표 | 관련 QA | 관련 AS | 검증 방법 | 프로토타입 제약 |
| :---: | ----- | :---: | :---: | ----- | ----- |
| IV-01 | 동시 입장 집중 구간에서 DB 커넥션 풀 사용률 80% 이하, 평균 응답시간 1초 이내 | QA-02 | AS-02, AS-04, AS-08 | k6 부하 · HikariCP activeConnections 메트릭 · 응답시간 분포 | C-06: 8만 명 축소 시뮬레이션 (단일 서버) |
| IV-02 | join-pool 고갈 시 service-pool 격리 — 회의 조회 API 성공률 100% 유지 | QA-03 | AS-08 | join-pool 의도적 포화 후 조회 API 동시 호출 · 성공률 확인 | — |
| IV-03 | 피크 시 권한 갱신 응답시간 1초 이내 — 캐시 hit율 90% 이상 | QA-01 | AS-03, AS-06 | k6 로그인 부하 · L1/L2 캐시 hit율 · 외부 호출 횟수 비교 | — |
| IV-04 | 외부 서버 장애 주입 시 핵심 기능 성공률 99.9% 이상 | QA-04, QA-05 | AS-09 | Stub 장애 주입 (응답 지연 / 오류) 후 입장 · 조회 API 성공률 측정 | C-05: 외부 서버 Stub 대체 |
<p align="center"><em>[표 62] 검증 항목 (IV) 목록</em></p>
