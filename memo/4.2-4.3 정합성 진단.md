# 4.2 · 4.3 정합성 진단 (수정 보류 상태)

docs 4.2/4.3 "보완" 요청에 따라 전 소절 정독 + 상위 문서(AS-09, 요구사항) 교차 대조 결과.
결론: **네 소절 모두 내용은 충실, 빈 곳 없음.** 대신 정합성·추적성 결함 4건 발견.

**완료 (2026-07-10)**
- 실제 목표: report 4.2/4.3을 A_Team 구성 수준으로 끌어올리기 → docs부터 재구성. 상세는 memory `project_42_restructure_plan.md`.
- ① 해소: docs·report 전부 Meeting Manager로 통일, wcServer 잔재 제거. ② 오진 확인(front-api 직접 연계는 MM·AC·Copilot 3개, WC/VC는 간접).
- ③ 해소: 4.2.3.3-data(docs)·표 75~77(report)에 Redis·Replica 이중화 명시로 RK-07 위임 책임 충족.
- ④ 해소: Overall C&C가 Primary/Replica 라우팅을 정적 흡수.
- UC-03/07 스테일 명칭(integration.wc/VC→meetingManager)도 docs·report 동기 교정.
- 잔여: report 다이어그램 drawio→PNG 렌더링(수작업). AS-09(3.2) WC/VC 명칭은 별도 안건.

## ① 외부 서버 명칭 불일치 (추적성) — 사용자 설명 대기

같은 CB(50%/10s, 회의 입장·개설 필수 서버)를 파일마다 다르게 표기.

| 위치 | 표기 |
|------|------|
| 4.2.1 시퀀스 participant | `Meeting Manager` |
| 4.2.1 시나리오5(1) CB 이름 (L222) | `wcServer CircuitBreaker` ← 한 파일 내 혼용 |
| 4.2.1 요약표 (L275) | "WC서버(50%, 10s)" |
| 4.2.2 / 4.2.3 | `integration.meetingManager` |
| 4.3.2 (L15,19) | `integration.wc`(WC서버), `integration.*`(VC/AC) |
| AS-09 상위 원본 | WC서버 / `integration.wc` / CB `wcServer` |

4.2 설계 문서에서 `WC서버 → Meeting Manager` 개명 흔적. 4.2.1 일부·4.3.2에 옛 이름 잔존, 상위 AS-09와도 불일치.
쟁점: AS-09 L84는 WC서버(회의 개설)와 Meeting Manager(입장 흐름)를 **다른 서버**처럼 기술 → 같은지 다른지 도메인 확인 필요.

## ② VC서버가 모듈 뷰에서 누락 — 사용자 설명 대기

- AS-09 원본 외부 서버: **WC·VC·AC·Copilot 4개**
- 4.2.2 모듈 뷰 `integration.*`: **meetingManager·ac·copilot 3개** → VC 흡수처 불명확

## ③ 이중화 책임 공백 (내부 모순) — 진단 완료, 수정 보류

- 4.3.4 RK-07: "각 컴포넌트 이중화·총량 설계가 배치 뷰(4.2.3)의 책임이 된다"고 명시 위임
- 4.2.3 배치 뷰: Redis·Replica를 **단일 노드로만** 표기, 이중화/HA 기술 전무
- → 위임받은 책임이 실제로는 비어 있음. 배치 뷰에 Redis(Sentinel/Cluster)·Replica 이중화 및 풀 총량 상한 근거 추가 필요.

## ④ AS-07 CQRS 런타임 시나리오 부재 — 진단 완료, 수정 보류

- 4.2.1 실행 뷰 5개 시나리오: UC-04 입장 / UC-01 권한 / AS-05 워밍 / AS-08 Bulkhead / AS-09 CB
- AS-07(readOnly 기반 Primary/Replica 라우팅) 런타임 흐름 없음 (배치·모듈 뷰에만 존재)
- → 의도적 생략 가능성 있음. 추가 시 UC-06 초대 등 read 경로 시퀀스로 표현 가능.

## 후속 작업 시 유의

- 수정 진행 시 docs(원본) ↔ report(파생) 동기 편집 필수
- mermaid 추가/삭제 시 memo/image-file-mapping.md 갱신
- 텍스트·표 수정 시 캡션·이미지 파일명·image-mapping 정합성 전수 점검
