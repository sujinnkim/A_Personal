server:
  port: 8090

spring:
  application:
    name: stub-server

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.example.stubserver: DEBUG
